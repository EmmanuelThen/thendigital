'use client'
import { ScrollArea } from '@radix-ui/react-scroll-area'
import React from 'react'
import ScrollingArea from '@/app/components/ScrollingArea'

type Props = {}

const DashOrderHistory = (props: Props) => {
  return (
    <div className='w-full'>
        <ScrollingArea />
    </div>
  )
}

export default DashOrderHistory