import React from 'react';
import EditTabs from '../components/EditTabs';

type Props = {}

const page = (props: Props) => {
  return (
    <main>
        <EditTabs />
    </main>
  )
}

export default page